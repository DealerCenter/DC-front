import React from 'react'
import ResetPassword from './ResetPassword'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
      <ResetPassword />
    </div>
  )
}

export default page
