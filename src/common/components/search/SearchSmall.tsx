import React from 'react'
import styled from 'styled-components'

type Props = {}

const SearchSmall = (props: Props) => {
  return <Container>SearchSmall</Container>
}

export default SearchSmall

const Container = styled.div``
